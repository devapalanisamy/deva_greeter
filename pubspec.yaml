name: greeter
description: A Greeter application.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  meta: ^1.1.8
  built_value: ^7.1.0
  built_collection: ^4.3.2
  rxdart: ^0.24.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.1
  build_runner: ^1.9.0
  built_value_generator: ^7.1.0
  pedantic: ^1.8.0+1

flutter:
  uses-material-design: true
